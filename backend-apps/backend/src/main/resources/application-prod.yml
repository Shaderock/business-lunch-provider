lunch:
  backend:
    url: ${BACKEND_URL}
    users:
      generate:
        default: false
  frontend:
    url: ${FRONTEND_URL}
  discovery:
    address: ${DISCOVERY_ADDRESS}
  database:
    address: ${DATABASE_ADDRESS}
